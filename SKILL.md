---
name: pengyuyan-ppt-skill
description: 将 PPT 需求文案、DOCX、PDF、TXT、XLSX、研究报告、业务材料或幻灯片设计蓝图，转成高信息密度、咨询式、可编辑的 PPTX。用户只要直接粘贴一段要做成 PPT 的文案、需求或业务材料，即使没有写“执行第一步”“帮我做 PPT”等任务提示词，也要自动触发本技能并默认从阶段 1 开始。用户提供一张或多张蓝图、设计图、设计稿、幻灯片截图，并表达“执行第三步”“还原”“转换/转成可编辑 PPT/PPTX”“按图制作”等意图时，直接走蓝图入口并执行阶段 3，不重复阶段 1 和风格探索。
---

<!-- ENCODING-GUARD: UTF-8. Chinese text must be reread as UTF-8 if garbled. -->

# PengyuyanPPT_Skill

## 目标

输出结论先行、证据可追溯、信息密度高、关键内容可编辑的专业 PPT。蓝图决定视觉，真实材料决定文字和数据。默认优先尽快交付可用 PPTX；只有用户明确要求像素级还原时才进入重型精修。

## 零提示词自动路由（最高优先级）

不要等待用户补充“使用本技能”“执行第一步”“开始制作”等任务口令。只要当前输入能合理判断为 PPT 内容或蓝图转换需求，就立即选择入口并开始对应流程。

### 默认进入阶段 1

满足任一条件时，自动视为“内容到 PPT”任务，并立即开始阶段 1：

- 用户直接粘贴一段 PPT 文案、页级内容、方案材料、汇报提纲、需求说明或业务文字；
- 用户上传 DOCX、PDF、TXT、XLSX、研究报告或业务材料，内容明显用于汇报、方案、路演、培训或演示；
- 用户只描述要做的 PPT 主题、受众、页数、场景或目标，没有写明确动作词；
- 用户给出标题、章节、表格、数据、案例等内容，但没有完整的逐页视觉蓝图。

此时直接读取阶段 1 所需 references，抽取证据并输出故事线/大纲确认稿。不得反问“你想让我做什么”，不得因为缺少“执行第一步”而停住。缺失信息不影响启动时，先基于现有材料推进，并把必要缺口列入阶段 1 确认项。

### 直接进入蓝图入口与阶段 3

同时满足“有完整蓝图输入”和“有转换意图”时，直接走蓝图入口：

1. 输入包含一张或多张完整幻灯片蓝图、设计图、设计稿、页面效果图、PPT 截图或 Image2/ImageGen 生成图；
2. 用户表达以下任一意图：
   - “执行第三步 / 第三阶段”；
   - “还原成可编辑 PPT / PPTX”；
   - “把蓝图/设计图/设计稿转成可编辑 PPT”；
   - “按图制作 / 按这个页面做成 PPT”；
   - “复刻、重建、转换、还原”并明确目标是 PPT/PPTX/可编辑幻灯片。

此时不要执行阶段 1、故事线发散、8 风格参考或重新生成蓝图。先做“蓝图入口的快速锁定”；若用户已明确“按此蓝图还原”，将已提供页面视为该范围内的已确认蓝图，确认目标页范围后进入阶段 3。单页蓝图任务不需要再要求用户确认同一张蓝图一次。

### 避免误判

- “参考这张图的风格”不等于蓝图转换；若用户同时给了正文材料并要求生成新 PPT，仍走阶段 1 → 阶段 2 → 阶段 3。
- 局部截图、Logo、配色图、图标样例、照片或非完整页面图片不算完整蓝图。
- 只有图片而没有任何 PPT 制作或转换意图，且无法从上下文判断目标时，才用一句话询问用途。
- 材料与完整蓝图同时存在且用户要求按图还原时，走蓝图入口：材料锁定事实，蓝图锁定视觉。

### 路由示例

| 用户输入 | 自动动作 |
|---|---|
| 直接粘贴一段企业方案文案 | 自动触发技能，从阶段 1 输出故事线与大纲确认稿 |
| “做一份面向管理层的 10 页 AI 项目汇报” | 自动触发技能，从阶段 1 开始 |
| 上传报告但未写任何命令 | 若内容明显是 PPT 素材，自动从阶段 1 开始 |
| 上传完整页面蓝图并说“转成可编辑 PPT” | 快速锁定蓝图，直接执行阶段 3 |
| 上传 PPT 截图并说“执行第三步” | 直接执行蓝图入口与阶段 3 |
| 上传参考图并说“参考这个风格，用我的文案做一套 PPT” | 参考图只锁定审美，仍从阶段 1 开始 |

## 先判断入口

| 入口 | 触发条件 | 执行路径 |
|---|---|---|
| A. 内容到 PPT | 用户提供文案、文件、数据或研究材料，没有完整蓝图；即使没有任务提示词也默认命中 | 阶段 1 → 阶段 2 → 阶段 3 |
| B. 蓝图到 PPT | 用户提供完整设计稿、幻灯片截图或 Image2/ImageGen 蓝图，并表达还原/转换为可编辑 PPT 的意图 | 快速锁定内容与风格 → 阶段 3 |

若材料和蓝图同时存在，以材料锁定事实，以蓝图锁定视觉。不要要求蓝图入口重复做故事线和 8 风格探索。

## 四个确认门

1. 内容入口：确认故事线、大纲、页数和每页信息密度。
2. 代表蓝图：先确认一张完整代表页的风格、字号、色块、图标、构图和信息密度，并固化 `visual_master`。
3. 整套蓝图：按已确认的 `visual_master` 生成并交付全部页面蓝图；只有用户明确回复“确认全部蓝图”或等价表达后，才允许进入阶段 3。
4. 阶段 3：先交付一张 A 版可编辑样板页并暂停，由用户第一次选择 A 或 B；该选择决定后续全部页面的 PPT 转换标准。

不得跨过必要确认门。代表蓝图确认只代表视觉方向锁定，不代表整套蓝图已确认。单页任务中，代表蓝图就是全部蓝图，用户确认该页即可通过第三个确认门。用户直接提供完整蓝图并明确“按此蓝图还原”时，可以把对应页面视为已通过蓝图确认，但仍需确认输入是否已覆盖全部目标页面。

## 阶段 1：证据、故事线与高密度页面规划

开始前完整读取：

- `references/source-analysis.md`
- `references/storyline.md`

必须：

- 从源材料抽取事实、数字、单位、期间、来源位置、冲突和 caveat。
- 不得用常识、蓝图文字或 ImageGen 数字补造事实。
- 提出 2–3 条可选故事线，比较证据强度、受众适配和风险，推荐一条。
- 用 SCR、issue tree 或 hypothesis tree 收敛论证。
- 每页写结论型标题、证据、图表计划、SO WHAT、信息区数量和组件清单。
- 保留 PengyuyanPPT_Skill 的高密度特征：页面应有清晰主论点、主体分析、证据/案例、解释层和结论层，而不是“标题 + 三张空卡片”。

向用户确认：故事线、页数、逐页论点、图表计划、密度和缺失数据。

## 阶段 2：开放风格探索与逐页蓝图

开始前完整读取 `references/visual-system.md`。

### 风格规则

- 8 种内置风格只是参考菜单，不是强制限制。
- 用户指定色彩、审美、品牌规范、参考图或已有蓝图时，以用户输入为最高优先级。
- 用户未指定方向时，可展示 `assets/palette-samples/` 的 8 张样张供参考，也可以基于内容推荐其中 2–3 种。
- 允许使用 Image2/ImageGen 生成任意新风格，不必落入既有 8 类。
- 仍需确认一个视觉方向后再批量生成逐页蓝图，避免批量时风格和信息密度漂移。

### 蓝图规则

- 蓝图必须覆盖已确认的真实内容、组件和信息密度。
- 每页完整 16:9，标题、主图、案例/证据、结论、页脚等位置明确。
- 蓝图是构图和视觉基准，不是最终事实来源。
- 先生成和确认一张完整代表蓝图；锁定后按同一 `visual_master` 生成剩余全部页面蓝图。
- 代表蓝图确认后，不得直接进入阶段 3；必须先交付整套蓝图并等待用户确认。
- 默认集中交付全部蓝图供一次性审阅；若用户明确要求逐页确认，才逐页暂停。
- 禁止为了方便阶段 3 而把高密度页面降级成低保真线框图。

### 整套蓝图交付与确认

代表蓝图确认后：

1. 冻结全局色板、字体层级、边距、标题区、页脚、SO WHAT、面板、图标风格和信息密度。
2. 生成剩余全部页面蓝图，不同时制作可编辑 PPT。
3. 一次性交付：
   - 每页独立高清蓝图；
   - 一张按页码排序的整套缩略图总览；
   - 页码、标题与蓝图文件对应清单。
4. 等待用户检查全部页面。用户可以一次性确认、指定个别页面修改，或要求某页更换构图。
5. 个别页面修改时只重做受影响蓝图；已确认页面不重做。
6. 只有用户明确回复“确认全部蓝图”或等价表达后，才设置 `blueprint_set_confirmed: true` 并进入阶段 3。

禁止一边生成剩余蓝图，一边制作 PPT。不得因为用户确认了代表页风格，就推定其确认了其他页面的构图。

## 蓝图入口的快速锁定

先完整读取 `references/visual-system.md`，但只执行其中“用户直接提供蓝图”的提取规则，不执行风格探索或逐页蓝图生成。

用户直接给蓝图时，只做以下轻量动作：

1. 识别画布比例、主色、字体层级、网格、边距、面板、图表、页眉页脚和图标风格。
2. 抽取蓝图中的可见文本；若另有源材料，以源材料校正事实。
3. 写一份精简 `slide_content_lock`：标题、关键数字、正文、图表数据、来源、SO WHAT。
4. 写一份精简 `visual_master`：全局视觉变量和可复用组件。
5. 确认用户提供的蓝图是否已覆盖全部目标页面：
   - 已覆盖全部页面：复述整套蓝图范围，用户确认后进入阶段 3；
   - 只覆盖部分页面：先补齐并确认剩余蓝图，不得提前进入阶段 3。

无需补做 8 风格样张、故事线发散或完整阶段 1。

## 阶段 3：快速还原为可编辑 PPTX

开始前完整读取：

- `references/ppt-production.md`
- `references/icon-strategy.md`
- `references/quality-assurance.md`

进入条件：`blueprint_set_confirmed: true`。内容入口必须完成整套蓝图确认；蓝图入口必须确认用户提供的蓝图已经覆盖全部目标页面。条件未满足时不得生成正式可编辑 PPTX。

### A/B 两段式质量档位

默认先生成 `fast / A`。A 不是低保真草稿：整体风格、颜色、构图、排版、信息密度、主要组件比例、阅读顺序和 P0 关键对象必须与蓝图基本一致。A 只是不自动追求最后约 10% 的图标细节、复杂曲线、阴影、圆角、微小间距和装饰一致性。

只有用户看过 A 版后选择 B，才进入 `standard / B` 的蓝图一致性精修。仅在用户明确提出“像素级、1:1、精确描摹、品牌/UI 绝对一致”时使用 `precision`。

| 档位 | 定义 | 默认验证 |
|---|---|---|
| `fast / A` | 风格、颜色、排版、结构和核心内容基本符合蓝图的 90% 可用版 | 能打开、无修复警告、结构兼容、关键内容可编辑、无明显缺失/乱码/溢出 |
| `standard / B` | 用户选择继续后，在 A 基础上做最后一段一致性精修 | PowerPoint 渲染、蓝图对照、P0/P1 修正、必要视觉复核 |
| `precision` | 像素级、品牌/UI、复杂核心几何 | 元素级测量、关键曲线追踪、局部差异图、严格门禁 |

### 开始阶段 3 时必须告诉用户

“整套蓝图已经确认。接下来先从已确认蓝图中选择一张代表页，制作 A 版可编辑样板。A 版会保证整体风格、颜色、排版、信息结构和核心内容基本一致，并完成 PowerPoint 打开与兼容性检查；交付后我会暂停，由你选择直接采用 A 标准，或继续做 B 版一致性精修。你的第一次 A/B 选择将用于后续全部页面。”

### 快速生产原则

1. 多张蓝图时，从中选择信息密度高、组件完整、能代表整套风格的页面作为样板；不必机械选择顺序上的第 1 页。
2. 样板页先建立 `visual_master`，后续页复用色板、字体、边距、标题区、页脚、SO WHAT、面板和常用组件。
3. 使用可复用高密度组件：步骤链、时间线、架构层、矩阵、案例卡、KPI、表格、对比条、结论条。
4. P0：标题、关键数字、主图/主流程、SO WHAT、品牌标识和用户点名区域。A 版必须基本准确。
5. P1：面板、卡片组、表格、次级图表。A 版按组件组匹配，保持构图、比例和阅读顺序。
6. P2：普通图标、装饰线、点阵、纹理、微小刻度。只需语义、风格、位置大体一致且不遮挡。
7. 主要文字、数字、表格和图表标签必须可编辑。不得把整页蓝图截图当背景。
8. 复杂视觉可混合使用 SVG、图片裁片或 custom geometry，但只保留必要区域，不能遮住可编辑信息层。
9. 普通图标不是高优先级：优先使用聚合图标库，找不到时用同风格、语义相近图标；只有 Logo、品牌符号和核心业务标识需要精确。
10. A 版修复所有 Critical/High，包括文件兼容问题和风格、构图、排版明显偏离；不因微小视觉差异自动启动完整精修。

### A 版交付暂停门

A 版生成后只执行最低兼容性与完整性检查：

- PPTX 能在 PowerPoint 中打开，且不提示修复；
- 页数、比例和画布正确；
- 没有文件损坏、无效对象边界或关键对象消失；
- 中文、字体和图标可正常显示；
- 关键文字、数字、表格和图表信息存在且可编辑；
- 没有明显乱码、严重溢出、遮挡或整页蓝图背景；
- 风格、颜色、构图、排版和信息密度基本符合蓝图。

通过后立即交付 A 版并停止。不要自动生成蓝图对照图、局部裁切、元素测量、独立视觉 QA 或多轮一致性修正。

必须询问用户：

“A. 当前效果满足，按 A 标准继续/归档；B. 继续做蓝图一致性精修，并按 B 标准继续。”

### 多蓝图批量规则

- 阶段 3 开始前，全部蓝图必须已经生成并确认。
- 未得到可编辑样板页的第一次 A/B 选择前，不制作剩余页面的可编辑 PPT。
- 用户选择 A：锁定样板页的 A 标准和 `visual_master`，把其余已确认蓝图按 A 标准批量转换为可编辑 PPT，不再逐页暂停；每页只做结构、兼容性、内容和可编辑性检查。
- 用户选择 B：先精修样板页并确认，把修改固化为 B 标准和 `visual_master`；再把其余已确认蓝图按 B 标准批量转换，不再重复询问 A/B。
- 只有用户明确要求逐页确认时，才恢复逐页暂停。
- 批量完成后合并；用户只对个别页面不满意时，只精修指定页面，不重做整套。

### 生成技术

- 使用工作区提供的 PPT 生成能力；正式页优先 PptxGenJS/pptx-generator。
- 可以使用 PowerPoint 原生形状、图表、文本和 SVG。
- 不得使用 `python-pptx` 作为正式页面生成引擎。
- 尺寸默认 16:9；统一主题字体并提供可用的中文回退字体。

## 图标总规则

- 优先级：项目缓存 → `assets/icons/index.json` 本地图标 → 可发现的开源 SVG 库 → PowerPoint/Fluent 原生图标 → 同风格语义近似 → 最后才自绘。
- 本地已含 `chunk-filled`、`tabler-filled`、`tabler-outline`、`phosphor-duotone`、`simple-icons`。
- 可扩展开源库：Lucide、Remix Icon、Heroicons、Material Symbols、Bootstrap Icons、Fluent UI System Icons、Carbon Icons。
- 一套 deck 锁定一个主要图标风格族；品牌 Logo 可例外使用 `simple-icons`。
- 不要求普通图标逐像素匹配蓝图，不做逐图标差异图或逐路径描摹。
- 使用 `scripts/select_icon.py` 检索、同步和缓存，不要把整个图标库读入上下文。

## 默认交付与 QA

A 版至少交付：

- 单页可编辑 PPTX；
- 文件打开与兼容性检查结果；
- 简短说明：A 版尚未执行最后一段蓝图一致性精修；
- A/B 选择提示。

A 版不默认交付渲染图、蓝图并排对照图、局部裁切、元素 registry、图标 bbox、像素差或独立视觉 QA。为验证打开和兼容性而临时产生的预览，不等于启动 B 阶段，也不必对用户展示。

B 版才要求 PowerPoint 渲染、蓝图对照、P0/P1 一致性检查和必要的修正证据。详细规则见 `references/quality-assurance.md`。

## 不可妥协项

- 不得编造数据、引用、Logo 或结论。
- 不得把整页蓝图或整页截图作为最终 PPT 页面背景。
- 不得为了速度删掉已确认的信息区、案例、表格、关键数字或 SO WHAT。
- 不得在用户未确认视觉方向时批量生成全部正式页。
- 不得在整套蓝图未确认时进入阶段 3。
- 不得一边生成剩余蓝图，一边制作可编辑 PPT。
- 不得把“确认代表蓝图风格”解释成“确认全部页面构图”。
- 不得把 A 版理解成低保真草稿；整体风格、颜色、排版、结构和信息密度必须基本符合蓝图。
- A 版通过兼容性检查后必须暂停，不得擅自启动 B 阶段。
- 不得因为普通图标找不到一模一样的版本而阻塞交付。

## 常用命令

```bash
python3 scripts/select_icon.py search \
  --index assets/icons/index.json \
  --query "knowledge search" \
  --style-family outline-business

python3 scripts/select_icon.py sync \
  --icons-root assets/icons \
  --icon tabler-outline/search \
  --out-dir project/icons

# 外部开源图标先加入聚合索引，再缓存到项目；Skill 不会自动联网下载
python3 scripts/select_icon.py cache \
  --index assets/icons/index.json \
  --icon lucide/search \
  --out-dir project/icons

# fast：不加 --strict；precision：加 --strict，并提供完整 manifest/visual QA
python3 scripts/validate_pptx.py deck.pptx --json-out qa.json
python3 scripts/validate_pptx.py deck.pptx \
  --manifest slide_manifest.json \
  --visual-qa visual_qa_gate.json \
  --strict --json-out qa.json
```

## 完成条件

单页 A 版可以在用户确认满意后直接完成并归档；不要求补做 B 阶段证据。

多页任务完成意味着：

- 整套蓝图已经由用户确认；
- 样板页已经确定 A 或 B 标准；
- 后续全部页面按该标准生成；
- 用户确认的内容与蓝图核心结构被保留；
- 关键文字、数字和图表信息可编辑；
- 最终合并版能在 PowerPoint 中正常打开、显示和编辑；
- A 标准不要求每页视觉对照；B 标准按一致性精修规则执行。
